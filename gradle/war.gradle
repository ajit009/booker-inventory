apply plugin: "war"

bootWar {
    mainClassName = "au.com.booker.BookerInventoryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
